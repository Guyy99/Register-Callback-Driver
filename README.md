Registry Callback Communication Driver

Allows UM/KM communication by setting a registry callback for any changed registry value to call a function in the kernel.
By changing a registry value from UM to our Instructions we want the kernel to execute, this can be picked up by the callback.
